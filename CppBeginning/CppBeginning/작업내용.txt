1. 기본 구조 코드 작성
#include <stdipo.h> 
int main()

2. 출력하기
printf("Hello, World!");

이니셜 출력(Hello World 대신 이니셜 출력)
printf("kjw"); 


1+1을 계산하는 프로그램을 만들기
printf("1 + 1 = %d\n", 1 + 1); 
└ 결과는 맞지만, 개발자로서는 틀림 → 상수로 미리 계산하면 컴퓨터는 놀았다.
   프로그램이라고 부를 수 없음. (적어도 숫자는 바꿔넣어야 프로그램이라 할 수 있음)

3. 변수 사용하기
int a; int b; → 변수의 초기화 (35, 99 대입)
a+b < printf("a + b = %d\n", a + b); → 
매개변수를 쓰다
└계산한 결과를 변수에 담아 전달하지 않기때문에 우선 정확한 값이 나오지 않는다.
└변수에 값이 a와 b를 입력한다. → c라는 변수에 a와 b의 값을 더한 결과를 담는다
└프린트가 꼭 필요한가? → 기능적으로는 문제가 없으나 결과 확인을 위해 필요
 실제 게임에서는 의도적으로 UI를 생략하기도 함(다만 프로그래머는 문제가 있는지 확인을 해야함)
 모든 변수를 출력하여 변수의 값이 문제가 없는지 항상 확인하는 습관 필요

 
 FunctionTestMain(a, b);
 매개 변수의 값에 따라 결과가 달라짐
 나누기를 할 때 문제가 발생 → 컴퓨터는 변수를 종류별로 다르게 표현
 int : 정수(자연수와 부호로 표기 가능한 수)
 float : 실수(0~1 사이의 값을 나타내는 수)
 나누기를 할 때 정수로 떨어지지 않는 값을 표현하기 위해 float 필요

 FunctionCalculatorMain();
 └ char //ASCII(미국표준) 코드표 → 256가지 문자표현
	한국어 표현 불가능
└char는 왜 썼는가? → 문자를 저장하기 위해 → 연산자 저장
└ %c : 문자형 입/출력

계산기에 입력받은 문자(연산자)에 따라 결과가 달라져야함.
└if 문, switch문 : case문
└ == : 같다
└ switch(변수) {case '값': 실행문; break;}
└ Add(int a, int b) == a + b → 반복될 때 함수를 사용


